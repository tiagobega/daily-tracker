# Supabase — runtime (públicas; a anon key é protegida pela RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase — somente server (NUNCA expor ao client; usada em rotinas futuras, ex.: push)
SUPABASE_SERVICE_ROLE_KEY=

# Connection string do Postgres — usada apenas pelo drizzle-kit (migrations)
DATABASE_URL=
